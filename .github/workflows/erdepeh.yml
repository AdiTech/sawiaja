name: Ubuntu-Linux-RDP-Fixed
on:
  workflow_dispatch:
    inputs:
      auth_command:
        description: 'Paste Google Auth Command (DISPLAY=...)'
        required: true
      pin:
        description: '6 Digit PIN'
        required: true
        default: '123456'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Update System Repositories
      run: |
        sudo apt-get update
    - name: Install Python Environment
      run: |
        sudo apt-get install -y python3 python3-pip python3-venv
    - name: Install Nodejs Environment
      run: |
        curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
        sudo apt-get install -y nodejs
    - name: Install Supporting Tools
      run: |
        sudo apt-get install -y wget curl git zip unzip tar
    - name: Install Desktop Environment
      run: |
        sudo apt-get install -y xfce4 desktop-base xfce4-terminal
    - name: Install Chrome Remote Desktop
      run: |
        wget https://dl.google.com/linux/direct/chrome-remote-desktop_current_amd64.deb
        sudo apt-get install -y ./chrome-remote-desktop_current_amd64.deb
    - name: Configure XFCE Session
      run: |
        sudo bash -c 'echo "exec /etc/X11/XSRV/Xsession /usr/bin/xfce4-session" > /etc/chrome-remote-desktop-session'
    - name: Initialize Google Connection
      run: |
        ${{ github.event.inputs.auth_command }} --pin=${{ github.event.inputs.pin }}
    - name: System Verification
      run: |
        python3 --version
        node --version
    - name: Keep Alive
      run: |
        sleep 21600
